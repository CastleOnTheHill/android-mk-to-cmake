[project]
name = "android-mk-to-cmake"
version = "0.3.0"
description = "Lightweight Makefile-to-CMake conversion experiments with a Python standard-library DAG executor."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = []
